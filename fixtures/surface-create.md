# 目标界面 Fixture: Create

## 用户输入

新增一个设置页，用来管理团队默认提示词。开始设计。

## 期望行为

- 触发 `bfds-design`。
- 判定改动类型为 `create`。
- 仍要确认目标界面是新页面还是现有设置页内新增 route。
- 询问或复用 `PRODUCT.md` / `DESIGN.md`。
- 不问 API、数据库或权限设计。

## Framing 摘要示例

目标界面: 新增团队默认提示词设置页
现状来源: 用户文字描述，低置信度；无现有页面
改动类型: create
必须保留: 目标项目 DESIGN.md、现有导航和设置页路由模式
允许改变: 页面信息层级、输入区结构、状态反馈、响应式密度
必须避免: 不新增权限模型，不编造团队数据，不设计后端接口
