# Design Brainstorm Fixture: Empty Style

## 输入

用户有 PRD，但项目还没有明确 `DESIGN.md`。

## 期望行为

- 优先调用或引导 Impeccable `init`，建立 `PRODUCT.md` 和 `DESIGN.md`。
- 如果用户暂不补上下文，明确低置信度并只问一个最高价值设计问题。
- 不直接凭“现代 SaaS”套色板。

## 可接受问题

这个 surface 的首要气质应更偏“高频操作的安静效率”，还是“新能力发布的明确引导”？

## 不可接受问题

API 返回什么字段？用户角色怎么设计？
